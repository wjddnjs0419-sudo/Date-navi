From 492e4e63a3c7454511db2d30f0789034a62be38f Mon Sep 17 00:00:00 2001
From: wjddnjs0419-sudo <ncf-ncglobal@ncf.or.kr>
Date: Tue, 28 Jul 2026 03:23:45 +0900
Subject: [PATCH] fix: normalize course budget per person

---
 __tests__/place-price.test.ts                 |  9 ++-
 .../recommend-date-ranking-server.test.ts     |  2 +-
 app/mode-flow/course-result.tsx               |  8 ++-
 docs/supabase-schema.sql                      | 69 +++++++++++++++++++
 locales/en/course.json                        |  8 +--
 locales/en/modeFlow.json                      |  1 +
 locales/ko/course.json                        |  8 +--
 locales/ko/modeFlow.json                      |  1 +
 shared/recommendation/place-price.ts          |  6 +-
 ...00_normalize_observed_price_per_person.sql | 69 +++++++++++++++++++
 10 files changed, 168 insertions(+), 13 deletions(-)
 create mode 100644 supabase/migrations/20260728100000_normalize_observed_price_per_person.sql

diff --git a/__tests__/place-price.test.ts b/__tests__/place-price.test.ts
index f6d76b2..2d3d1fb 100644
--- a/__tests__/place-price.test.ts
+++ b/__tests__/place-price.test.ts
@@ -10,8 +10,13 @@ import {
 } from '../shared/recommendation/place-price';
 
 describe('priceAnchorKRW', () => {
-  it('예산을 코스 장소 수로 나눈 몫이 앵커다', () => {
-    expect(priceAnchorKRW(30000, 3)).toBe(10000);
+  it('2인 총 코스 예산을 1인·장소당 앵커로 바꾼다', () => {
+    // UI의 1인 전체 예산 3만원은 Edge 계약에서 2인 총액 6만원으로 전달된다.
+    // AI 추정 가격도 1인 기준이므로 2개 장소면 장소당 1만5천원과 비교해야 한다.
+    expect(priceAnchorKRW(60000, 2)).toBe(15000);
+  });
+  it('2인 총 예산을 코스 장소 수와 인원수로 나눈 몫이 앵커다', () => {
+    expect(priceAnchorKRW(30000, 3)).toBe(5000);
   });
   it('장소 수 0 이하나 예산 없음은 null', () => {
     expect(priceAnchorKRW(30000, 0)).toBeNull();
diff --git a/__tests__/recommend-date-ranking-server.test.ts b/__tests__/recommend-date-ranking-server.test.ts
index ce36478..43d730a 100644
--- a/__tests__/recommend-date-ranking-server.test.ts
+++ b/__tests__/recommend-date-ranking-server.test.ts
@@ -453,7 +453,7 @@ describe('rankPlaceCandidates — budget 점수', () => {
   );
 
   it('예산과 가격이 모두 있으면 몫 이내 장소가 가점, 몫을 크게 넘는 장소가 감점을 받는다', () => {
-    const ranked = rankPlaceCandidates([placeA, placeB], { ...request(), totalBudgetKRW: 30000 }, {
+    const ranked = rankPlaceCandidates([placeA, placeB], { ...request(), totalBudgetKRW: 60000 }, {
       limit: 10,
       prices: new Map([
         ['a', priceFields({ estimatedMinKRW: 5000, estimatedMaxKRW: 9000 })],
diff --git a/app/mode-flow/course-result.tsx b/app/mode-flow/course-result.tsx
index 1f944a8..c0c9a1b 100644
--- a/app/mode-flow/course-result.tsx
+++ b/app/mode-flow/course-result.tsx
@@ -508,7 +508,13 @@ export default function CourseResultScreen() {
               <Text style={s.conditionText}>{snapshot.request.location.label}</Text>
               <Text style={s.conditionText}>{snapshot.request.courseSteps.map((step) => step.label).join(' → ')}</Text>
               {snapshot.request.maxWalkingMinutes && <Text style={s.conditionText}>{snapshot.request.maxWalkingMinutes} min</Text>}
-              {snapshot.request.totalBudgetKRW && <Text style={s.conditionText}>{snapshot.request.totalBudgetKRW.toLocaleString()} KRW</Text>}
+              {snapshot.request.totalBudgetKRW && (
+                <Text style={s.conditionText}>
+                  {t('modeFlow.courseResult.perPersonCourseBudget', {
+                    amount: (snapshot.request.totalBudgetKRW / 2).toLocaleString(),
+                  })}
+                </Text>
+              )}
               {snapshot.response.course.relaxedConstraints.map((item) => <Text key={item.constraint} style={s.relaxedText}>{item.reason}</Text>)}
             </View>
           )}
diff --git a/docs/supabase-schema.sql b/docs/supabase-schema.sql
index ccd95ab..2d4db81 100644
--- a/docs/supabase-schema.sql
+++ b/docs/supabase-schema.sql
@@ -1713,3 +1713,72 @@ begin
   if position('candidate_pool = candidate_pool,' in lower(v_definition)) = 0 then raise exception 'candidate pool immutability injection failed'; end if;
 end;
 $$;
+
+-- ============================================================================
+-- 20260728100000_normalize_observed_price_per_person.sql
+-- totalBudgetKRW는 2인 코스 총액이고 places 가격 범위는 1인·장소당 기준이다.
+-- ============================================================================
+
+create or replace function public.recompute_place_observed_price(p_kakao_place_id text)
+returns void language plpgsql security definer set search_path = public, pg_temp as $$
+declare
+  v_min integer; v_max integer; v_samples integer;
+  v_min_samples integer; v_max_samples integer;
+begin
+  with couple_answers as (
+    select distinct on (pf.couple_id)
+      pf.price_level,
+      round(((rs.original_request ->> 'totalBudgetKRW')::numeric)
+        / 2
+        / greatest((select count(*) from public.recommendation_course_steps cs where cs.session_id = pf.session_id), 1)
+      )::integer as anchor_krw
+    from public.place_feedback pf
+    join public.recommendation_sessions rs on rs.id = pf.session_id
+    where pf.kakao_place_id = p_kakao_place_id
+      and pf.price_level is not null
+      and pf.couple_id is not null
+      and (rs.original_request ->> 'totalBudgetKRW') ~ '^[0-9]+$'
+    order by pf.couple_id, pf.updated_at desc
+  ),
+  sorted as (
+    select
+      array_agg(anchor_krw order by anchor_krw) filter (where price_level = 3) as lowers,
+      array_agg(anchor_krw order by anchor_krw) filter (where price_level = 1) as uppers,
+      count(*)::integer as sample_count
+    from couple_answers
+  ),
+  bounds as (
+    select
+      case when lowers is null then null else lowers[floor((array_length(lowers, 1) - 1) * 0.75)::integer + 1] end as min_krw,
+      case when uppers is null then null else uppers[ceil((array_length(uppers, 1) - 1) * 0.25)::integer + 1] end as max_krw,
+      sample_count,
+      coalesce(array_length(lowers, 1), 0) as min_sample_count,
+      coalesce(array_length(uppers, 1), 0) as max_sample_count
+    from sorted
+  )
+  select
+    case when b.min_krw is not null and b.max_krw is not null and b.min_krw > b.max_krw then null else b.min_krw end,
+    case when b.min_krw is not null and b.max_krw is not null and b.min_krw > b.max_krw then null else b.max_krw end,
+    b.sample_count, b.min_sample_count, b.max_sample_count
+  into v_min, v_max, v_samples, v_min_samples, v_max_samples
+  from bounds b;
+
+  update public.places set
+    observed_min_krw = v_min,
+    observed_max_krw = v_max,
+    observed_sample_count = coalesce(v_samples, 0),
+    observed_min_sample_count = coalesce(v_min_samples, 0),
+    observed_max_sample_count = coalesce(v_max_samples, 0),
+    updated_at = now()
+  where kakao_place_id = p_kakao_place_id;
+end;
+$$;
+revoke all on function public.recompute_place_observed_price(text) from public, anon, authenticated;
+
+do $$
+declare r record;
+begin
+  for r in select kakao_place_id from public.places loop
+    perform public.recompute_place_observed_price(r.kakao_place_id);
+  end loop;
+end $$;
diff --git a/locales/en/course.json b/locales/en/course.json
index 364eec9..76c3ef0 100644
--- a/locales/en/course.json
+++ b/locales/en/course.json
@@ -37,9 +37,9 @@
       }
     },
     "budget": {
-      "label": "Budget per person (KRW, optional)",
+      "label": "Whole-course budget per person (KRW, optional)",
       "amount": "KRW {{amount}}",
-      "hint": "Enter the budget per person, up to KRW 100,000."
+      "hint": "Enter what one person will spend for the whole course, up to KRW 100,000."
     },
     "moods": {
       "label": "Preferred moods (select any)",
@@ -72,7 +72,7 @@
       "step_count_invalid": "Build a course with two to four steps.",
       "duplicate_step_ids": "Two course steps have the same identifier. Please reopen this screen.",
       "invalid_step_category": "One of the course steps is not supported.",
-      "budget_invalid": "The budget per person must be a number from KRW 1 to 10,000,000.",
+      "budget_invalid": "The whole-course budget per person must be a number from KRW 1 to 10,000,000.",
       "additional_request_too_long": "Keep the additional request to 500 characters or fewer.",
       "exclusion_conflict": "Your selected steps conflict with an exclusion: {{categories}}. Edit the text or the steps."
     },
@@ -84,7 +84,7 @@
       "category": "Select {{category}} for step {{number}}",
       "walking": "Select a maximum walk of {{option}}",
       "mood": "Select the {{mood}} mood",
-      "budget": "Enter the per-person budget in Korean won",
+      "budget": "Enter the whole-course budget per person in Korean won",
       "duration": "Select total time",
       "additionalRequest": "Enter an additional request",
       "generate": "Build the date course"
diff --git a/locales/en/modeFlow.json b/locales/en/modeFlow.json
index c8506a7..f665f6a 100644
--- a/locales/en/modeFlow.json
+++ b/locales/en/modeFlow.json
@@ -121,6 +121,7 @@
         "unsupported": "‘{{term}}’ couldn't be applied to this course"
       },
       "conditions": "View applied conditions",
+      "perPersonCourseBudget": "Whole-course budget per person: KRW {{amount}}",
       "editError": "We couldn't save this course change. Please try again.",
       "confirm": "Confirm",
       "regenerate": "Regenerate",
diff --git a/locales/ko/course.json b/locales/ko/course.json
index 8a758af..5e61b1b 100644
--- a/locales/ko/course.json
+++ b/locales/ko/course.json
@@ -37,9 +37,9 @@
       }
     },
     "budget": {
-      "label": "1인 예산 (KRW, 선택)",
+      "label": "1인 전체 코스 예산 (KRW, 선택)",
       "amount": "{{amount}}원",
-      "hint": "한 사람 기준 예산을 입력해주세요. 최대 100,000원"
+      "hint": "한 사람이 전체 코스에서 쓸 예산을 입력해주세요. 최대 100,000원"
     },
     "moods": {
       "label": "원하는 분위기 (복수 선택 가능)",
@@ -72,7 +72,7 @@
       "step_count_invalid": "코스는 2~4단계로 구성해주세요.",
       "duplicate_step_ids": "코스 단계 식별자가 겹쳤어요. 화면을 다시 열어주세요.",
       "invalid_step_category": "지원하지 않는 코스 단계가 있어요.",
-      "budget_invalid": "1인 예산은 1원 이상 10,000,000원 이하의 숫자로 입력해주세요.",
+      "budget_invalid": "1인 전체 코스 예산은 1원 이상 10,000,000원 이하의 숫자로 입력해주세요.",
       "additional_request_too_long": "추가 요청은 500자 이하로 입력해주세요.",
       "exclusion_conflict": "선택한 단계와 제외 요청이 충돌해요: {{categories}}. 텍스트나 단계를 수정해주세요."
     },
@@ -84,7 +84,7 @@
       "category": "{{number}}단계 카테고리 {{category}} 선택",
       "walking": "최대 도보 시간 {{option}} 선택",
       "mood": "분위기 {{mood}} 선택",
-      "budget": "1인 예산 원화 입력",
+      "budget": "1인 전체 코스 예산 원화 입력",
       "duration": "전체 시간 선택",
       "additionalRequest": "추가 요청 입력",
       "generate": "데이트 코스 만들기"
diff --git a/locales/ko/modeFlow.json b/locales/ko/modeFlow.json
index 253dd36..2d3707d 100644
--- a/locales/ko/modeFlow.json
+++ b/locales/ko/modeFlow.json
@@ -121,6 +121,7 @@
         "unsupported": "‘{{term}}’은(는) 이 코스 구성에 반영하지 못했어요"
       },
       "conditions": "적용한 조건 보기",
+      "perPersonCourseBudget": "1인 전체 코스 예산 {{amount}}원",
       "editError": "코스 변경을 저장하지 못했어요. 다시 시도해주세요.",
       "confirm": "코스 확정",
       "regenerate": "잠금 외 재추천",
diff --git a/shared/recommendation/place-price.ts b/shared/recommendation/place-price.ts
index ad98d78..4270f1c 100644
--- a/shared/recommendation/place-price.ts
+++ b/shared/recommendation/place-price.ts
@@ -6,6 +6,10 @@
 export const PRICE_LEVEL = { cheap: 1, normal: 2, expensive: 3 } as const;
 export type PriceLevel = (typeof PRICE_LEVEL)[keyof typeof PRICE_LEVEL];
 
+// totalBudgetKRW는 UI의 1인 예산을 두 배로 바꾼, 커플 2인의 코스 총액 계약이다.
+// 장소 가격 원장의 모든 금액은 1인 기준이므로 앵커도 1인·장소당으로 정규화한다.
+export const COUPLE_PARTICIPANT_COUNT = 2;
+
 // 미결정 사항(스펙): 안쪽 백분위 값. 실측 분포 확인 전까지의 초기값.
 // ⚠️ SQL이 이 상수를 import할 수 없어 같은 값이 두 곳에 산다. 여기를 바꾸면
 // public.recompute_place_observed_price(마이그레이션 20260727140000, docs/supabase-schema.sql)의
@@ -19,7 +23,7 @@ export type ObservedBounds = { minKRW: number | null; maxKRW: number | null; con
 export function priceAnchorKRW(totalBudgetKRW: number | null | undefined, stepCount: number): number | null {
   if (!totalBudgetKRW || !Number.isFinite(totalBudgetKRW) || totalBudgetKRW <= 0) return null;
   if (!Number.isFinite(stepCount) || stepCount <= 0) return null;
-  const anchor = Math.round(totalBudgetKRW / stepCount);
+  const anchor = Math.round(totalBudgetKRW / COUPLE_PARTICIPANT_COUNT / stepCount);
   // 0원 앵커는 모든 장소를 예산 초과로 만든다. 몫이 0으로 반올림될 만큼
   // 작은 예산은 앵커가 없는 것과 같게 다룬다(점수 0).
   return anchor > 0 ? anchor : null;
diff --git a/supabase/migrations/20260728100000_normalize_observed_price_per_person.sql b/supabase/migrations/20260728100000_normalize_observed_price_per_person.sql
new file mode 100644
index 0000000..2d33f85
--- /dev/null
+++ b/supabase/migrations/20260728100000_normalize_observed_price_per_person.sql
@@ -0,0 +1,69 @@
+-- totalBudgetKRW는 2인 코스 총액, places.*_krw는 1인 장소당 금액이다.
+-- 기존 관측 앵커가 2인·장소당으로 저장된 단위 오류를 정정하고 전체 원장을 재계산한다.
+begin;
+
+create or replace function public.recompute_place_observed_price(p_kakao_place_id text)
+returns void language plpgsql security definer set search_path = public, pg_temp as $$
+declare
+  v_min integer; v_max integer; v_samples integer;
+  v_min_samples integer; v_max_samples integer;
+begin
+  with couple_answers as (
+    select distinct on (pf.couple_id)
+      pf.price_level,
+      round(((rs.original_request ->> 'totalBudgetKRW')::numeric)
+        / 2
+        / greatest((select count(*) from public.recommendation_course_steps cs where cs.session_id = pf.session_id), 1)
+      )::integer as anchor_krw
+    from public.place_feedback pf
+    join public.recommendation_sessions rs on rs.id = pf.session_id
+    where pf.kakao_place_id = p_kakao_place_id
+      and pf.price_level is not null
+      and pf.couple_id is not null
+      and (rs.original_request ->> 'totalBudgetKRW') ~ '^[0-9]+$'
+    order by pf.couple_id, pf.updated_at desc
+  ),
+  sorted as (
+    select
+      array_agg(anchor_krw order by anchor_krw) filter (where price_level = 3) as lowers,
+      array_agg(anchor_krw order by anchor_krw) filter (where price_level = 1) as uppers,
+      count(*)::integer as sample_count
+    from couple_answers
+  ),
+  bounds as (
+    select
+      case when lowers is null then null else lowers[floor((array_length(lowers, 1) - 1) * 0.75)::integer + 1] end as min_krw,
+      case when uppers is null then null else uppers[ceil((array_length(uppers, 1) - 1) * 0.25)::integer + 1] end as max_krw,
+      sample_count,
+      coalesce(array_length(lowers, 1), 0) as min_sample_count,
+      coalesce(array_length(uppers, 1), 0) as max_sample_count
+    from sorted
+  )
+  select
+    case when b.min_krw is not null and b.max_krw is not null and b.min_krw > b.max_krw then null else b.min_krw end,
+    case when b.min_krw is not null and b.max_krw is not null and b.min_krw > b.max_krw then null else b.max_krw end,
+    b.sample_count, b.min_sample_count, b.max_sample_count
+  into v_min, v_max, v_samples, v_min_samples, v_max_samples
+  from bounds b;
+
+  update public.places set
+    observed_min_krw = v_min,
+    observed_max_krw = v_max,
+    observed_sample_count = coalesce(v_samples, 0),
+    observed_min_sample_count = coalesce(v_min_samples, 0),
+    observed_max_sample_count = coalesce(v_max_samples, 0),
+    updated_at = now()
+  where kakao_place_id = p_kakao_place_id;
+end;
+$$;
+revoke all on function public.recompute_place_observed_price(text) from public, anon, authenticated;
+
+do $$
+declare r record;
+begin
+  for r in select kakao_place_id from public.places loop
+    perform public.recompute_place_observed_price(r.kakao_place_id);
+  end loop;
+end $$;
+
+commit;
--
2.50.1 (Apple Git-155)
